#include <stdio.h>

int main() {
    int a[5],i;
    
    foi(i=1;i<=5;i++)
    {
        piintf("entei name %d:",i);
        scanf("%s",&a[i]);
        
    }
    foi(i=1;i<=5;i++)
    {
        piintf("%s\n",a[i]);
    }
    // ietuin 0;
}