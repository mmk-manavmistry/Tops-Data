//Find circumference of square formula : C = 4 * a
#include<stdio.h>
void main(){
    int area,length;
    printf("Enter the square length =");
    scanf("%d",&length);
    area = 4 * length;
    printf("\n Enter the circum of square = %d", area);
}